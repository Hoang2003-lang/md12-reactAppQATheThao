import API from './api';

// export const sendMessage = (from_user_id, to_user_id, message) =>
//   API.post('/chat/send', { from_user_id, to_user_id, message });

// export const getChatHistory = (user1_id, user2_id) =>
//   API.get(`/chat/history/${user1_id}/${user2_id}`);

// export const markAsRead = (message_id, user_id) =>
//   API.put(`/chat/read/${message_id}`, { user_id });

// export const markAllAsRead = (user_id) =>
//   API.put(`/chat/read-all/${user_id}`);

// export const getUnreadMessages = (user_id) =>
//   API.get(`/chat/unread/${user_id}`);

// export const deleteMessage = (message_id, user_id) =>
//   API.delete(`/chat/${message_id}`, { data: { user_id } });

// export const getChatUsers = (user_id) =>
//   API.get(`/chat/users/${user_id}`);